<!DOCTYPE html>
<link rel="icon"
      type="image/png"
      href="/img/favicon.ico" />
<html>
  <head>
    <link rel="stylesheet" href="/css/main.css">
  </head>
  <body>
    <div class="centered">
      <h1>Choose SSO Provider</h1>
      <p>Email: {{ .email }}</p>
      <a href="{{ .link }}"><button>Login with Google!</button></a>
    </div>
  </body>
</html>
